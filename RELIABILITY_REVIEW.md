**Reliability work and findings for review — 2026-09-14**

Backup: `G:\DeadlockBehavior-backup-before-reliability-20260912`. SHA-256 verified 4,542 files / 10,749,693,828 bytes before application edits. Verification is recorded in the backup's `BACKUP_VERIFICATION.json`.

The implementation preserves the research notebook, validated all original 108 A metrics, and has now promoted 14 narrowly supported B metrics. The current contract is 122 A metrics: 102 core and 20 extended. All promotions require evidence, producer ownership, availability, and display/export coverage.

| Finding | Action / review status |
|---|---|
| Pregame included in alive/dead, health and firing aggregates | Gameplay integration and primary-fire events exclude time below zero; regression tests check time partitions and baselines. |
| Fixed 30-second team tick reconstruction | Uses the replay's observed offset; non-30-second fixture tested. |
| Checkpoints beyond the replay | Only reached checkpoints are emitted. |
| Missing telemetry converted to zero/dead | Explicit nullable fields, unknown-state duration, source/field availability, strict JSONL parsing. |
| Duplicate player names merged | Model and UI use controller-based player IDs; melee producer grouping and evidence identity filters corrected. |
| Next-primary-ready read obsolete fields and looked beyond scrubber | Shared metric evaluator uses production readiness timeline at or before selected time. |
| A mapping count presented without source validity | Availability is separate from wiring, with per-player reasons and published-run provenance. |
| Stale contract did not trigger processing | Exact metric IDs, input fingerprints, and output hashes gate readiness; failed unchanged inputs require deliberate retry. |
| Partial runs could mix old and new artifacts | Isolated workspaces, immutable published run directories, manifest publication after validation. Flat compatibility files remain for historical scripts; the visualizer reads the published snapshot. |
| Stale disk/in-memory model | Model cache includes schema, code and input fingerprints; requests recheck before reusing cache. |
| Melee/scoreboard evidence routes stale | Dedicated runtime melee and player-state scoreboard routes; controller-scoped filtering. |
| Existing producer-count test expected nine | Updated to ten; current root suite passes. |
| Full sample stream reduced to one-second samples in UI | State queries now retain the observed sample cadence, avoiding omitted within-second changes. |
| Final fractional second of team advantage omitted | Team series includes observed fractional end boundary. |
| Some legacy A summary cards bypassed availability | Fixed: canonical cards use the shared evaluator; legacy A summaries fail closed when the published run or required field is unavailable. |
| Comparison panels reset distinct same-name/player selections | Fixed: workspace persistence and validation use stable controller-based player IDs. |
| No visible retry for a failed or stale replay | Fixed: Process / retry invokes the same serialized production path as import and the filesystem watcher. |
| B metric `ground_soul_vacuum_target` mixed research matching with a broad label | Promoted narrowly as per-player resolved first physical `m_hVacuumTarget` links. Across all seven local replays, 12,181/12,183 links resolved (99.98%); two stale identities remain unresolved. All seven AssignedGold-index negative controls resolved zero. This is not economic receipt, collection, payout, ownership, reward value, or last-hit identity. |
| Direct weapon-state summaries compared interleaved replacement-weapon rows | Fixed: reload and counter transitions are computed independently per weapon entity before player aggregation. The independent validator recomputes every transition from the published JSONL. |
| A player with no PrimaryWeapon observations caused replay failure or risked a false zero | Fixed: the replay source remains authoritative when its observed players meet all carrier gates; an unobserved player/time is explicitly `N/A`. `rep02` contains one such player and passes this behavior end to end. |
| A stale visualizer server and CLI validator could publish competing runs | Fixed: an atomic per-replay filesystem lock now rejects cross-process contention and safely removes locks left by dead processes. Server-side queue coalescing still handles duplicates inside one process. |
| Three B weapon-state labels implied broader semantics than the carriers establish | Promoted with narrow definitions: observed reload state/transitions, raw numeric active-fire-mode value, and raw continuous/burst counters. Reload cause/efficiency, mode names, trigger intent, burst design, ammo, projectiles, damage, and effective-weapon composition remain excluded. |
| Trooper context numeric conversion treated `null` as zero | Fixed in the production extractor and shared Trooper helper. Missing direct context remains `null`, and every promoted event must contain integer `m_nSubclassID`, `m_iTeamNum`, and `m_iLane` values before publication. |
| B Trooper base-type and team/lane labels mixed direct fields with inferred names | Promoted narrowly as raw numeric subclass-ID counts and raw numeric team/lane-pair counts. Named Ranged/Medic/Melee types, lane names, jungle status, and Normal/Super/Rift variants remain B research. Across nine replays, all 34,610 death rows had complete direct context and reconciled exactly to their summaries. |
| B flying-soul labels mixed lifecycle telemetry with outcome semantics | Promoted `trooper_orbs` and `orb_attackable_window`: mutually unique same-team links from the validated raw economic-Trooper subclass set to CItemXP subclass 494398941 inside the frozen -1..+4 tick / 250 HU envelope, plus direct attackable-time differences. Across nine replays, all 6,816 accepted episodes had complete nonnegative attackable windows. Secure/deny, rewards, automatic awards, visibility, and opportunity remain B. |
| Flying-soul production initially used the prior Trooper position at the death transition | Fixed to use the current death-transition position. The corrected producer exactly reproduced the five established strict-link counts: 600, 681, 1,080, 753, and 648. |
| Six B flying-soul damage labels implied attempts or outcomes beyond their evidence | Promoted with narrowed definitions: observed-damage/no-observed-damage episode distribution, selected-player damaged episodes, multiple-message episodes, multiple-player episodes, mixed-team episodes, and selected-player damage-message count. The producer requires exact victim entity, a tick inside the episode-relative direct attackable window, and a unique sampled-player pawn/controller/team identity. Across nine replays it observed 894 messages in 707 of 6,816 episodes, with zero ambiguous episode links or unresolved attackers, and reproduced all 26 established exact-window research hits. No-observed-damage does not mean miss or ignored opportunity; the metrics do not establish projectile causality, secure/deny, winner, reward, visibility, or opportunity. |
| Windows compatibility-copy rename could fail with `EPERM` while the visualizer read a JSONL file | Added bounded retry and a direct-copy fallback for flat historical compatibility files. Immutable production-run publication and manifest-last switching remain authoritative. |
| Twelve inspector UI tests depended on launching Node from `inspector-v04` | Fixed: test paths now derive from each module URL. The complete inspector suite passes from the repository root. |
| Original broad Ground Soul resolver promotion candidate | HELD FOR REVIEW: V03 deterministic recovery and no-reuse gates do not independently prove economic attribution; negative/control evidence is still needed. |
| Official/pause-adjusted game clock | OPEN SCIENTIFIC LIMIT: current axis is observed demo ticks minus the observed offset; official pause-adjusted clock authority is not established. |
| Resource compatibility across future game patches | OPEN SCIENTIFIC LIMIT: frozen resource contracts are fingerprinted and producer gates reject unknown carriers; unseen builds still require validation. |
| Prediction readiness | OPEN: final-match aggregates are explicitly retrospective. A future feature dataset needs observation-time cutoffs, stable player identity across matches, and held-out player/match/build evaluation. |

The backup includes pre-existing uncommitted research outputs. They were not reset. A review finding does not automatically authorize a scientific promotion.

Final validation: all nine local replays published 122/122 (`104373259`, `104559948`, `105367926`, `test`, and `rep01`–`rep05`). The reliability validator passed 108 players and 39,528 start/mid/end probes with no errors. The Trooper context audit passed 34,610 death rows with four observed raw subclass IDs, two teams, three lane values, and six team/lane pairs. The flying-soul lifecycle audit passed 6,816 mutually unique source-linked episodes with 100% direct attackable-window coverage and exact agreement with all five established source-link counts. The flying-soul damage audit passed 894 player-attributed messages in 707 episodes, including 166 multiple-message, 61 multiple-player, and 40 mixed-team episodes, with all 26 established exact-window hits reproduced. The direct weapon-state replication audit passed 335,734 event rows, 12,680 reload entries and exits, 1,298 raw fire-mode changes, 303,656 continuous-count changes, and 49,862 burst-remaining changes.

Validation command: `node scripts/228-validate-production-reliability.mjs --process <replay-name> ...`. Reports go to `output/cross_replay/production_reliability_*_v02.json`. Promotion replication reports include `output/cross_replay/ground_soul_physical_target_promotion_validation_v01.json`, `output/cross_replay/primary_weapon_direct_state_promotion_validation_v01.json`, `output/cross_replay/runtime_trooper_context_promotion_validation_v01.json`, `output/cross_replay/runtime_flying_soul_promotion_validation_v01.json`, and `output/cross_replay/runtime_flying_soul_damage_promotion_validation_v01.json`. The validator checks the entire canonical A set for every player at start, midpoint and end, plus time, movement, melee, scoreboard and team reconciliations. `available=false` and a measured zero are distinct.
