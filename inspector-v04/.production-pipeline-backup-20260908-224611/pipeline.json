{
  "version": "INSPECTOR_REPLAY_PROCESSING_MANIFEST_V01",
  "stopOnRequiredFailure": true,
  "notes": [
    "This manifest is intentionally explicit. Historical numbered research scripts are not automatically run as a production ETL.",
    "Add only current/replay-safe processing entrypoints as they are consolidated. The UI exposes every step result instead of silently fabricating missing metrics."
  ],
  "steps": []
}
