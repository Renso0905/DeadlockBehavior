import{createReadStream,existsSync,mkdirSync,readFileSync,writeFileSync}from'node:fs';
import{basename,dirname,extname,resolve}from'node:path';
import{createInterface}from'node:readline';
import{EntityOperation,InterceptorStage,Logger,Parser,ParserConfiguration}from'deadem';
import{requireClaim}from'../../src/contracts/claim-registry.mjs';
import{TROOPER_FLYING_SOUL_SUBCLASS_ID,associatePlayerDamage,buildStrictTrooperLinks,extractPlayerDamageReference,isFlyingSoulRelaunch,readEntityWorldPosition,startFlyingSoulEpisode,summarizeFlyingSoulLinks,updateFlyingSoulEpisode}from'../lib/runtime-flying-soul.mjs';

const VERSION='RUNTIME_FLYING_SOUL_PRODUCTION_V01',STATUS='RUNTIME_FLYING_SOUL_PRODUCTION_V01_READY',TICK_RATE=64;
const arg=process.argv[2];if(!arg)throw new Error('Usage: node inspector-v04/production/extract-runtime-flying-souls.mjs <replay.dem>');
const replayPath=resolve(arg),replayName=basename(replayPath,extname(replayPath)),dir=resolve('output',replayName);
const playerSummaryPath=resolve(dir,'player_state_summary.json'),playerStatePath=resolve(dir,'player_state.jsonl'),trooperPath=resolve(dir,'runtime_trooper_death_events_v01.jsonl');
const outputPath=resolve(dir,'runtime_flying_soul_production_v01.json'),eventsPath=resolve(dir,'runtime_flying_soul_events_v01.jsonl');
for(const p of[replayPath,playerSummaryPath,playerStatePath,trooperPath])if(!existsSync(p))throw new Error(`Required input missing: ${p}`);
const claim=requireClaim('runtime_flying_soul_direct_lifecycle_v01',{requireSemantic:true,requireReplication:true});
const damageClaim=requireClaim('runtime_flying_soul_damage_observation_v01',{requireSemantic:true,requireReplication:true});
const playerSummary=JSON.parse(readFileSync(playerSummaryPath,'utf8')),offset=finite(playerSummary.matchClockOffsetSeconds)??0;
const deaths=await readJsonl(trooperPath),playerByPawn=await readPlayerPawnMap(playerStatePath),previous=new Map(),open=new Map(),sequence=new Map(),episodes=[],damageMessages=[];
let candidateEntityEvents=0,replayEndTick=0;
const parser=new Parser(new ParserConfiguration({entityClasses:['CItemXP']}),Logger.CONSOLE_INFO);
parser.registerPostInterceptor(InterceptorStage.DEMO_PACKET,p=>{if(Number.isFinite(p?.tick))replayEndTick=Math.max(replayEndTick,p.tick);});
parser.registerPostInterceptor(InterceptorStage.MESSAGE_PACKET,(packet,messagePacket)=>{const tick=finite(packet?.tick);if(tick===null)return;const damage=extractPlayerDamageReference(messagePacket);if(damage)damageMessages.push({tick,...damage});});
parser.registerPostInterceptor(InterceptorStage.ENTITY_PACKET,(packet,_message,rows)=>{const tick=finite(packet?.tick);if(tick===null)return;replayEndTick=Math.max(replayEndTick,tick);
  for(const row of rows){const entity=row.entity;if(!entity||String(entity?.class?.name??'')!=='CItemXP')continue;candidateEntityEvents++;
    const index=finite(entity.index);if(index===null)continue;
    if(row.operation===EntityOperation.DELETE){const ep=open.get(index);if(ep){ep.endTick=tick;ep.endReason='ENTITY_DELETE';episodes.push(ep);open.delete(index);}previous.delete(index);continue;}
    if(row.operation!==EntityOperation.CREATE&&row.operation!==EntityOperation.UPDATE)continue;
    const current={tick,entityIndex:index,subclassId:String(safeField(entity,'m_nSubclassID')??'UNKNOWN'),team:finite(safeField(entity,'m_iTeamNum')),position:readEntityWorldPosition(entity),launchNum:finite(safeField(entity,'m_nLaunchNum')),timeLaunch:finite(safeField(entity,'m_timeLaunch')),attackableTime:finite(safeField(entity,'m_flAttackableTime')),endAttackableTime:finite(safeField(entity,'m_flEndAttackableTime'))};
    const prior=previous.get(index)??null,active=open.get(index)??null;
    if(active&&isFlyingSoulRelaunch(prior,current,active)){active.endTick=tick-1;active.endReason='ENTITY_RELAUNCH_OR_REUSE';episodes.push(active);open.delete(index);}
    let ep=open.get(index);if(!ep){const n=(sequence.get(index)??0)+1;sequence.set(index,n);ep=startFlyingSoulEpisode(current,n);open.set(index,ep);}updateFlyingSoulEpisode(ep,current);previous.set(index,current);
  }
});
try{await parser.parse(createReadStream(replayPath));}finally{await parser.dispose();}
for(const ep of open.values()){ep.endTick=ep.lastObservedTick;ep.endReason='REPLAY_END_CENSORED';episodes.push(ep);}episodes.sort((a,b)=>a.startTick-b.startTick||a.entityIndex-b.entityIndex||a.sequence-b.sequence);
const linkResult=buildStrictTrooperLinks(deaths,episodes),linkedEvents=linkResult.strictLinks.map((link,i)=>{const e=link.episode;const attackableWindowSeconds=finite(e.endAttackableTime)!==null&&finite(e.attackableTime)!==null?finite(e.endAttackableTime)-finite(e.attackableTime):null;return{...e,schemaVersion:'runtime_flying_soul_event_v02',replay:replayName,eventIndex:i,matchStartTimeSeconds:e.startTick/TICK_RATE-offset,attackableWindowSeconds,sourceLink:{method:'MUTUALLY_UNIQUE_SAME_TEAM_TICK_AND_DISTANCE',deathEntityIndex:link.deathEntityIndex,deathTick:link.deathTick,tickDelta:link.tickDelta,distance3D:link.distance3D},semanticStatus:'OBSERVED_SOURCE_LINKED_TROOPER_CITEMXP_EPISODE'};});
const damage=associatePlayerDamage(linkedEvents,damageMessages,playerByPawn,{matchClockOffsetSeconds:offset,tickRate:TICK_RATE}),events=damage.events;
const summary={...summarizeFlyingSoulLinks(events),damageObservation:damage.summary},duplicateIds=events.length-new Set(events.map(e=>e.episodeId)).size;
const sufficientlyLong=(finite(playerSummary.finalMatchTimeSeconds)??0)>=300;
const invalidDamagePlayerContext=events.flatMap(e=>e.playerDamageMessages).filter(x=>!x.attackerPlayer?.playerId||!Number.isInteger(x.attackerPlayer?.controllerEntityIndex)||![2,3].includes(x.attackerPlayer?.team)).length;
const checks={claimCurrent:check(claim.authorityStatus,'current',claim.authorityStatus==='current'),semanticPass:check(claim.semanticValidation,'pass',claim.semanticValidation==='pass'),replicated:check(claim.replicationStatus,'cross_replay_replicated',claim.replicationStatus==='cross_replay_replicated'),damageClaimCurrent:check(damageClaim.authorityStatus,'current',damageClaim.authorityStatus==='current'),damageSemanticPass:check(damageClaim.semanticValidation,'pass',damageClaim.semanticValidation==='pass'),damageReplicated:check(damageClaim.replicationStatus,'cross_replay_replicated',damageClaim.replicationStatus==='cross_replay_replicated'),episodesObservedWhenEligible:check(events.length,sufficientlyLong?'>0':'not required before 5:00',!sufficientlyLong||events.length>0),damageObservedWhenEligible:check(summary.damageObservation.totalObservedPlayerDamageMessages,sufficientlyLong?'>0':'not required before 5:00',!sufficientlyLong||summary.damageObservation.totalObservedPlayerDamageMessages>0),uniqueEpisodeIds:check(duplicateIds,0,duplicateIds===0),allSubclassDirect:check(events.filter(e=>e.subclassId!==TROOPER_FLYING_SOUL_SUBCLASS_ID).length,0,events.every(e=>e.subclassId===TROOPER_FLYING_SOUL_SUBCLASS_ID)),allLinksInsideEnvelope:check(events.filter(e=>e.sourceLink.tickDelta< -1||e.sourceLink.tickDelta>4||e.sourceLink.distance3D>250).length,0,events.every(e=>e.sourceLink.tickDelta>=-1&&e.sourceLink.tickDelta<=4&&e.sourceLink.distance3D<=250)),completeAttackableWindows:check(summary.missingAttackableWindows,0,summary.missingAttackableWindows===0),nonnegativeAttackableWindows:check(events.filter(e=>!(e.attackableWindowSeconds>=0)).length,0,events.every(e=>e.attackableWindowSeconds>=0)),unambiguousDamageEpisodeLinks:check(damage.diagnostics.ambiguousEpisodeMatches,0,damage.diagnostics.ambiguousEpisodeMatches===0),completeDamageAttackerAttribution:check(damage.diagnostics.unresolvedAttacker,0,damage.diagnostics.unresolvedAttacker===0),completeDamagePlayerContext:check(invalidDamagePlayerContext,0,invalidDamagePlayerContext===0),damageMessageReconciliation:check(summary.damageObservation.totalObservedPlayerDamageMessages,damage.diagnostics.matchedToAttackableEpisode,summary.damageObservation.totalObservedPlayerDamageMessages===damage.diagnostics.matchedToAttackableEpisode)};
const failed=Object.entries(checks).filter(([,v])=>!v.pass).map(([k])=>k);if(failed.length)throw new Error(`Flying-soul production integrity failed: ${failed.join(', ')}`);
const output={version:VERSION,canonical:false,createdAt:new Date().toISOString(),status:STATUS,authorityLayer:'extended',replay:{replayName,replayPath,ticksPerSecond:TICK_RATE,matchClockOffsetSeconds:offset,replayEndTick,finalMatchTimeSeconds:finite(playerSummary.finalMatchTimeSeconds)},foundation:{claimId:claim.claimId,damageClaimId:damageClaim.claimId,replicationStatus:damageClaim.replicationStatus},semanticScope:{supported:`Observed CItemXP subclass ${TROOPER_FLYING_SOUL_SUBCLASS_ID} episodes connected to the validated raw economic-Trooper subclass set only when the same-team tick/distance candidate is mutually unique, direct attackable-time windows, and player-attributed k_EUserMsg_Damage observations inside those windows.`,notClaimed:['Every Trooper death produces a flying soul','Human-readable Trooper type or variant names','Unlinked CItemXP meaning','Secure or deny outcome or winner','Reward recipient or value','Automatic awards','Projectile causality or shooting accuracy','Continuous visibility or player opportunity']},counts:{candidateEntityEvents,allCitemxpEpisodes:episodes.length,rawPlayerDamageMessages:damageMessages.length,...linkResult,strictLinks:events.length},summary,damageDiagnostics:damage.diagnostics,validation:{pass:true,checks}};
delete output.counts.strictLinks;output.counts.strictLinks=events.length;
mkdirSync(dirname(outputPath),{recursive:true});writeFileSync(outputPath,JSON.stringify(output,null,2)+'\n');writeFileSync(eventsPath,events.map(x=>JSON.stringify(x)).join('\n')+(events.length?'\n':''));
console.log(`Status: ${STATUS}`);console.log(`Source-linked episodes: ${events.length}`);console.log(`Attackable windows: ${summary.attackableWindows}/${events.length}`);console.log(`JSON: ${outputPath}`);

function safeField(entity,name){try{return entity.getField(name);}catch{return undefined;}}
function finite(v){if(v===null||v===undefined||v==='')return null;const n=Number(v);return Number.isFinite(n)?n:null;}
function check(actual,expected,pass){return{actual,expected,pass:Boolean(pass)};}
async function readJsonl(path){const out=[];const rl=createInterface({input:createReadStream(path,{encoding:'utf8'}),crlfDelay:Infinity});for await(const line of rl)if(line.trim())out.push(JSON.parse(line));return out;}
async function readPlayerPawnMap(path){const map=new Map(),ambiguous=new Set();const rl=createInterface({input:createReadStream(path,{encoding:'utf8'}),crlfDelay:Infinity});for await(const line of rl){if(!line.trim())continue;const row=JSON.parse(line),pawnEntityIndex=finite(row?.pawn?.entityIndex),controllerEntityIndex=finite(row?.controller?.entityIndex);if(pawnEntityIndex===null||controllerEntityIndex===null)continue;const player={playerId:`controller:${controllerEntityIndex}`,controllerEntityIndex,pawnEntityIndex,playerName:String(row?.controller?.playerName??`Controller ${controllerEntityIndex}`),team:finite(row?.controller?.team),heroId:finite(row?.controller?.heroId)};const prior=map.get(pawnEntityIndex);if(prior&&prior.controllerEntityIndex!==controllerEntityIndex){ambiguous.add(pawnEntityIndex);map.delete(pawnEntityIndex);}else if(!ambiguous.has(pawnEntityIndex))map.set(pawnEntityIndex,player);}return map;}
